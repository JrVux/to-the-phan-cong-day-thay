import schoolData from './schoolData.json'

export const seedTeachers = schoolData.teachers
export const seedPeriods = schoolData.schedule_periods
export const seedSchedules = schoolData.schedules
export const seedAssignments = schoolData.assignments
export const seedSubstitutions = schoolData.substitutions
export const seedLocks = schoolData.teacher_locks

export const seedData = schoolData
